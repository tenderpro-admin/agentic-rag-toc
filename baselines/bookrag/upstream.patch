diff --git a/.gitignore b/.gitignore
index 32827a1..d436a9b 100644
--- a/.gitignore
+++ b/.gitignore
@@ -205,3 +205,9 @@ test/
 /Scripts/cfg/MMLongBench.yaml
 /Scripts/cfg/Qasper.yaml
 /config/tmp/*
+
+# FinanceBench local harness artifacts
+/datasets/*.json
+/runs/
+/results/
+/metrics/
diff --git a/Core/provider/embedding.py b/Core/provider/embedding.py
index 580d1b3..13bcb22 100644
--- a/Core/provider/embedding.py
+++ b/Core/provider/embedding.py
@@ -10,8 +10,7 @@ import json
 
 from abc import ABC, abstractmethod
 
-from modelscope import AutoTokenizer, AutoModel
-from transformers import AutoModel as transformer_AutoModel
+from transformers import AutoModel, AutoTokenizer
 import ollama
 import openai
 import torch.nn.functional as F
@@ -71,7 +70,7 @@ class GmeEmbeddingProvider(BaseEmbedder):
         log.info(f"Loading model: {self.model_name}...")
 
         try:
-            self.model = transformer_AutoModel.from_pretrained(
+            self.model = AutoModel.from_pretrained(
                 self.model_name,
                 torch_dtype="auto",
                 trust_remote_code=True,
@@ -461,15 +460,21 @@ class TextEmbeddingProvider(BaseEmbedder):
 
         if self.backend == "local":
             if device == "auto":
-                self.device = "cuda" if torch.cuda.is_available() else "cpu"
+                if torch.cuda.is_available():
+                    self.device = "cuda"
+                else:
+                    self.device = (
+                        "mps"
+                        if hasattr(torch.backends, "mps") and torch.backends.mps.is_available()
+                        else "cpu"
+                    )
             else:
                 self.device = device
 
-            log.info(f"Using local backend (ModelScope) on device: {self.device}")
+            log.info(f"Using local Hugging Face backend on device: {self.device}")
             log.info(f"Loading model: {self.model_name}...")
 
             try:
-                # 根据官方示例，使用AutoTokenizer和AutoModel，并设置padding_side
                 self.tokenizer = AutoTokenizer.from_pretrained(
                     self.model_name, padding_side="left", trust_remote_code=True
                 )
@@ -486,7 +491,9 @@ class TextEmbeddingProvider(BaseEmbedder):
         elif self.backend == "ollama":
             self.device = "ollama_service"
         elif self.backend == "openai":
-            self.client = openai.OpenAI(api_key="empty", base_url=api_base)
+            self.client = openai.OpenAI(
+                api_key=os.environ.get("OPENAI_API_KEY", "empty"), base_url=api_base
+            )
         else:
             raise ValueError(
                 f"Unsupported backend: '{self.backend}'. Choose 'local' or 'ollama'."
diff --git a/Core/provider/llm.py b/Core/provider/llm.py
index f353edc..2f9e9d2 100644
--- a/Core/provider/llm.py
+++ b/Core/provider/llm.py
@@ -1,8 +1,20 @@
 from typing import Dict, Optional, Any, List, Union
 from abc import ABC, abstractmethod
 from openai import OpenAI
+import os
 import ollama
 import logging
+
+
+def _resolve_api_key(configured: Optional[str]) -> Optional[str]:
+    """Use OPENAI_API_KEY from env when the config key is a placeholder.
+
+    Lets configs ship a sentinel (env/openai/empty/None) instead of a real
+    secret; falls back to the configured value for genuine local servers.
+    """
+    if configured in (None, "", "env", "openai", "empty", "None"):
+        return os.environ.get("OPENAI_API_KEY", configured)
+    return configured
 from pydantic import BaseModel
 from concurrent.futures import ThreadPoolExecutor, as_completed
 from Core.Common.Memory import Memory
@@ -77,10 +89,21 @@ class OpenAIController(BaseLLMController):
             else 0.0
         )
 
+        api_key = _resolve_api_key(llm_config.api_key)
         if base_url is not None:
-            self.client = OpenAI(api_key=llm_config.api_key, base_url=base_url)
+            self.client = OpenAI(api_key=api_key, base_url=base_url)
         else:
-            self.client = OpenAI(api_key=llm_config.api_key)
+            self.client = OpenAI(api_key=api_key)
+
+        # `chat_template_kwargs` (the Qwen thinking-mode toggle) is a vLLM-only
+        # extension; the real OpenAI API rejects it with 400 "Unknown parameter".
+        # Only send it to a local vLLM endpoint, not to api.openai.com.
+        self._send_template_kwargs = bool(base_url) and "openai.com" not in (base_url or "")
+
+        # OpenAI reasoning models (gpt-5*, o1*, o3*, o4*) reject custom temperature
+        # (only default 1) and frequency/presence_penalty. Omit those for them.
+        m = (self.model or "").lower()
+        self._is_reasoning = m.startswith(("gpt-5", "o1", "o3", "o4"))
 
     def _prepare_messages(
         self, prompt_or_memory: Union[str, Memory], add_system_prompt: bool = True
@@ -105,12 +128,20 @@ class OpenAIController(BaseLLMController):
         parameters = {
             "model": self.model,
             "messages": messages,
-            "temperature": self.temperature,
-            "max_tokens": get_max_output_tokens(messages, self.max_tokens),
-            "frequency_penalty": self.frequency_penalty,
-            "presence_penalty": self.presence_penalty,
-            "extra_body": {"chat_template_kwargs": {"enable_thinking": False}},
         }
+        max_out = get_max_output_tokens(messages, self.max_tokens)
+        if self._is_reasoning:
+            # reasoning models use max_completion_tokens, not max_tokens
+            parameters["max_completion_tokens"] = max_out
+            # minimal reasoning = near-zero reasoning tokens, much faster/cheaper
+            parameters["reasoning_effort"] = "minimal"
+        else:
+            parameters["max_tokens"] = max_out
+            parameters["temperature"] = self.temperature
+            parameters["frequency_penalty"] = self.frequency_penalty
+            parameters["presence_penalty"] = self.presence_penalty
+        if self._send_template_kwargs:
+            parameters["extra_body"] = {"chat_template_kwargs": {"enable_thinking": False}}
         if json_response:
             parameters["response_format"] = {"type": "json_object"}
         response = self.client.chat.completions.create(**parameters)
@@ -161,15 +192,18 @@ class OpenAIController(BaseLLMController):
                     )
             messages = [{"role": "user", "content": content_list}]
 
-        completion = self.client.beta.chat.completions.parse(
-            temperature=self.temperature,
+        parse_kwargs = dict(
             model=self.model,
             messages=messages,
             response_format=schema,
-            extra_body={
-                "chat_template_kwargs": {"enable_thinking": think_mode},
-            },
         )
+        if not self._is_reasoning:
+            parse_kwargs["temperature"] = self.temperature
+        else:
+            parse_kwargs["reasoning_effort"] = "minimal"
+        if self._send_template_kwargs:
+            parse_kwargs["extra_body"] = {"chat_template_kwargs": {"enable_thinking": think_mode}}
+        completion = self.client.beta.chat.completions.parse(**parse_kwargs)
 
         if completion.usage:
             tracker = TokenTracker.get_instance()
diff --git a/Core/provider/rerank.py b/Core/provider/rerank.py
index d3aae30..d497e6d 100644
--- a/Core/provider/rerank.py
+++ b/Core/provider/rerank.py
@@ -1,6 +1,11 @@
 from Core.Index.Graph import Entity
+import os
 import torch
-from modelscope import AutoModelForCausalLM, AutoTokenizer
+from transformers import (
+    AutoModelForCausalLM,
+    AutoModelForSequenceClassification,
+    AutoTokenizer,
+)
 from typing import List, Dict, Optional
 import math
 from tqdm import tqdm
@@ -63,8 +68,16 @@ class TextRerankerProvider:
         # 本地后端逻辑 (将原有代码移入此分支)
         # ==========================================================
         elif self.backend == "local":
+            self.sequence_classifier = "bge-reranker" in model_name.lower()
             if device == "auto":
-                self.device = "cuda" if torch.cuda.is_available() else "cpu"
+                if torch.cuda.is_available():
+                    self.device = "cuda"
+                else:
+                    self.device = (
+                        "mps"
+                        if hasattr(torch.backends, "mps") and torch.backends.mps.is_available()
+                        else "cpu"
+                    )
             else:
                 self.device = device
 
@@ -72,28 +85,39 @@ class TextRerankerProvider:
             log.info(f"Loading reranker model: {self.model_name}...")
 
             # 加载分词器，Qwen Reranker要求左填充
+            offline = (
+                os.environ.get("HF_HUB_OFFLINE") == "1"
+                or os.environ.get("TRANSFORMERS_OFFLINE") == "1"
+                or os.path.isabs(model_name)
+            )
             self.tokenizer = AutoTokenizer.from_pretrained(
-                model_name, padding_side="left"
+                model_name, padding_side="left", local_files_only=offline
             )
 
             # 加载模型
-            model_args = {"trust_remote_code": True}
+            model_args = {"trust_remote_code": True, "local_files_only": offline}
             if "cuda" in self.device:
                 model_args["torch_dtype"] = torch_dtype
-                if use_flash_attention:
-                    try:
-                        model_args["attn_implementation"] = "flash_attention_2"
-                        log.info("Using Flash Attention 2.")
-                    except ImportError:
-                        log.info(
-                            "Flash Attention 2 not available. Falling back to default attention."
-                        )
-
-            self.model = (
-                AutoModelForCausalLM.from_pretrained(model_name, **model_args)
-                .to(self.device)
-                .eval()
+                # The original try/except wrapped a dict assignment that can never
+                # raise ImportError, so it forced flash_attention_2 and crashed in
+                # from_pretrained when flash_attn isn't installed. Probe for real.
+                import importlib.util
+
+                if use_flash_attention and importlib.util.find_spec("flash_attn") is not None:
+                    model_args["attn_implementation"] = "flash_attention_2"
+                    log.info("Using Flash Attention 2.")
+                else:
+                    model_args["attn_implementation"] = "sdpa"
+                    log.info("flash_attn not installed; using SDPA attention.")
+
+            model_class = (
+                AutoModelForSequenceClassification
+                if self.sequence_classifier
+                else AutoModelForCausalLM
             )
+            self.model = model_class.from_pretrained(model_name, **model_args).to(
+                self.device
+            ).eval()
 
             log.info("Reranker model loaded successfully.")
 
@@ -334,6 +358,26 @@ class TextRerankerProvider:
 
 
         elif self.backend == "local":
+            if self.sequence_classifier:
+                all_scores = []
+                for i in range(0, len(documents), batch_size):
+                    batch = [[query, doc] for doc in documents[i : i + batch_size]]
+                    inputs = self.tokenizer(
+                        batch,
+                        padding=True,
+                        truncation=True,
+                        max_length=self.max_length,
+                        return_tensors="pt",
+                    ).to(self.device)
+                    with torch.no_grad():
+                        logits = self.model(**inputs).logits
+                    if logits.shape[-1] == 1:
+                        scores = torch.sigmoid(logits[:, 0])
+                    else:
+                        scores = torch.softmax(logits, dim=-1)[:, -1]
+                    all_scores.extend(scores.cpu().tolist())
+                return all_scores
+
             # 1. 创建所有的查询-文档对
             pairs = [
                 self._format_instruction(query, doc, instruction) for doc in documents
diff --git a/Core/provider/vlm.py b/Core/provider/vlm.py
index 1741208..66d00ca 100644
--- a/Core/provider/vlm.py
+++ b/Core/provider/vlm.py
@@ -157,10 +157,13 @@ class GPTVLMController(BaseVLMController):
         from openai import OpenAI
 
         self.model_name = config.model_name or "gpt-4o"
+        api_key = config.api_key
+        if api_key in (None, "", "env", "openai", "empty", "None"):
+            api_key = os.environ.get("OPENAI_API_KEY", api_key)
         self.client = (
-            OpenAI(api_key=config.api_key, base_url=config.api_base)
+            OpenAI(api_key=api_key, base_url=config.api_base)
             if config.api_base
-            else OpenAI(api_key=config.api_key)
+            else OpenAI(api_key=api_key)
         )
         self.temperature = config.temperature or 0.1
         self.max_tokens = config.max_tokens or 1000
diff --git a/Core/rag/gbc_rag.py b/Core/rag/gbc_rag.py
index 45e58a5..f455328 100644
--- a/Core/rag/gbc_rag.py
+++ b/Core/rag/gbc_rag.py
@@ -110,7 +110,12 @@ class GBCRAG(BaseRAG):
         res_list = []
         for ent_str in entities_str:
             query_res = self.gbc_index.entity_vdb.search(query_text=ent_str, top_k=2)
-            min_distance = query_res[0]["distance"] if query_res else float("inf")
+            # entity vdb can return nothing -> query_res[0] would IndexError.
+            # (line below already had the `if query_res` guard but the metadata
+            # accesses did not.) Skip unmappable entities instead of crashing.
+            if not query_res:
+                continue
+            min_distance = query_res[0]["distance"]
             retrieve_name = query_res[0]["metadata"].get("entity_name")
             retrieve_type = query_res[0]["metadata"].get("entity_type")
             node_name = self.gbc_index.GraphIndex.get_node_name_from_str(
diff --git a/Core/utils/table_utils.py b/Core/utils/table_utils.py
index 1386a62..bc5e72b 100644
--- a/Core/utils/table_utils.py
+++ b/Core/utils/table_utils.py
@@ -153,7 +153,17 @@ def create_hierarchical_headers(
         num_cols = len(header_grid[0]) if header_grid else 0
 
         for j in range(num_cols):
-            path_parts = [header_grid[i][j].strip() for i in range(num_header_rows)]
+            # header rows can be ragged (a later header row shorter than the
+            # first) -> header_grid[i][j] would IndexError. Skip cells that don't
+            # exist in shorter rows instead of crashing the whole doc's inference.
+            # ragged rows: j may exceed a shorter row -> guard len.
+            # None cells: parse_html_table_to_grid fills unmerged slots with None
+            # -> guard with `or ""` so .strip() never hits NoneType.
+            path_parts = [
+                (header_grid[i][j] or "").strip()
+                for i in range(num_header_rows)
+                if j < len(header_grid[i])
+            ]
             unique_ordered_parts = list(dict.fromkeys(p for p in path_parts if p))
 
             if unique_ordered_parts:
diff --git a/main.py b/main.py
index 314fab4..e3be343 100644
--- a/main.py
+++ b/main.py
@@ -1,5 +1,6 @@
 import logging
 import argparse
+import os
 import pandas as pd
 import yaml
 from pathlib import Path
@@ -249,6 +250,11 @@ def main():
     log.info("------------------------\n")
 
     base_system_cfg: SystemConfig = load_system_config(args.config)
+    if model_name := os.environ.get("BOOKRAG_LLM_MODEL"):
+        base_system_cfg.llm.model_name = model_name
+    if reranker_name := os.environ.get("BOOKRAG_RERANKER_MODEL"):
+        base_system_cfg.graph.reranker_config.model_name = reranker_name
+        base_system_cfg.rag.strategy_config.reranker_config.model_name = reranker_name
 
     token_tracker = TokenTracker.get_instance()
     token_tracker.reset()
@@ -270,11 +276,9 @@ def main():
             df = pd.read_json(dataset_cfg.dataset_path)
             print(f"Dataset shape: {df.shape}")
         except FileNotFoundError:
-            log.error(f"ERROR: Dataset file not found at '{dataset_cfg.dataset_path}'")
-            return
+            raise SystemExit(f"Dataset file not found at '{dataset_cfg.dataset_path}'")
         except Exception as e:
-            log.error(f" ERROR: Failed to parse JSON file. Reason: {e}")
-            return
+            raise SystemExit(f"Failed to parse JSON file: {e}") from e
 
         # 2. Group by document identifiers to find unique documents
         document_groups = df.groupby(["doc_uuid", "doc_path"])
@@ -285,10 +289,9 @@ def main():
         total_docs = len(all_groups)
 
         if args.num > args.nsplit or args.num <= 0:
-            print(
+            raise SystemExit(
                 f"  - ERROR: --num ({args.num}) must be between 1 and --nsplit ({args.nsplit})."
             )
-            return
 
         # Calculate the start and end index for the current split
         items_per_split = math.ceil(total_docs / args.nsplit)
@@ -371,11 +374,12 @@ def main():
                         dataset_name=dataset_name,
                     )
                 except Exception as e:
+                    import traceback as _tb
                     log.error(f"  - ERROR: Failed to run inference. Reason: {e}")
+                    log.error("  - TRACEBACK:\n" + _tb.format_exc())
                     rag_error_list.append((doc_uuid, str(e)))
 
         # get the base directory of the script
-        import os
         base_dir = os.path.dirname(os.path.abspath(__file__))
         dataset_name = dataset_cfg.dataset_name
         timestamp = time.strftime("%Y%m%d_%H%M%S")
@@ -402,6 +406,8 @@ def main():
             log.info(
                 f"  - RAG completed with errors. See {error_log_path} for details."
             )
+        if index_error_list or rag_error_list:
+            raise SystemExit("One or more documents failed; see the error log above")
         log.info(f"--- All documents in this split processed. ---\n")
     else:
         # SINGLE FILE MODE
