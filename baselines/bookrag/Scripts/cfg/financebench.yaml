# Dataset config for the FULL FinanceBench set (built by the DVC `dataset` stage
# from params.yaml `docs`, e.g. docs: all -> all 84 PDFs). The boeing cfg next to
# this one stays for the single-doc smoke.
dataset_path: ../datasets/financebench.json
working_dir: ../runs/financebench
dataset_name: financebench
